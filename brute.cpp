#include <bits/stdc++.h>
using namespace std;

int main() {
	int a,b;
	string[i] = {"Zero" , "One", "Two", "Three" , "Four" , "Five" , "Six" , "Seven" , "Eight" , "Nine"}; 
	cin >> a >> b;

	for (int i=a; i <= b; i++) {
		if (i%2 == 0) {
			cout << "even" << endl;
		} else if (i%2 != 0) {
			cout << "odd" << endl;
		}
	} else {
		cout << represent[i] << endl;
	}
	return 0;
}